#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin >> s;
    vector<vector<int >> a;
    for(int i = 0; i < s.size(); ++ i){
        if(s[i] == 'Q' || s[i] == 'M')  cout<<'\n';
        cout << s[i];
    }
}

